<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="view_whatsapp_telegram_config" model="ir.ui.view">
        <field name="name">whatsapp.telegram.config.form</field>
        <field name="model">res.config.settings</field>
        <field name="arch" type="xml">
            <form string="Configuración de WhatsApp y Telegram">
                <group>
                    <field name="whatsapp_api_token"/>
                    <field name="whatsapp_phone_id"/>
                </group>
                <group>
                    <field name="telegram_bot_token"/>
                </group>
                <footer>
                    <button string="Guardar" type="object" name="execute" class="btn-primary"/>
                    <button string="Cancelar" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="action_whatsapp_telegram_config" model="ir.actions.act_window">
        <field name="name">Configuración WhatsApp y Telegram</field>
        <field name="res_model">res.config.settings</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
        <field name="view_id" ref="view_whatsapp_telegram_config"/>
    </record>

    <menuitem id="menu_whatsapp_telegram_config"
              name="Configuración Mensajería"
              parent="base.menu_config"
              action="action_whatsapp_telegram_config"/>
</odoo>